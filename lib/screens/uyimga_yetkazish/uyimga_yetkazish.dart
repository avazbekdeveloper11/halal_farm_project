import 'package:flutter/material.dart';

class UyimgaYetkazish extends StatelessWidget {
  const UyimgaYetkazish({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
